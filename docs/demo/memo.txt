--- 対応済み ---
①要件定義書を用意しておき、ワンショットで動くようにしたいので、プロンプトは3つもいらないですね
  → 3プロンプト構成を廃止。GAME_SPEC.md + ワンショットプロンプトに変更済み。

②こちらの第一用途はWebinar広告です。なので1分くらいでまとめて欲しいかも（何倍速かはします）
  → フロー（03_flow.md）を約1分構成に変更。倍速編集の目安も記載済み。

③Pjtセットアップもない状態から一気に生成される方が見ごたえある
  → 空のディレクトリ + GAME_SPEC.md のみの状態からスタートする構成に変更。
    プロンプトも「プロジェクトのセットアップから」を含む形に更新済み。

④最終的にゲームをAIがプレイしている図まで取りたい
  → プロンプトに「AI自動プレイモードの追加 + 動作確認」を組み込み。
    仕様書を読んで実装 → 自動プレイで動作確認、という流れに。
    フロー（03_flow.md）にPhase 4「AIプレイ」をクライマックスとして追加済み。

⑤Vercelデプロイは不要、開発環境（localhost）で確認する
  → 全ドキュメントからVercel関連を削除。npm run dev + localhost で即確認する構成に変更済み。

⑥masterにデモ準備一式を置き、デモのたびにブランチを切って実行する運用
  → master = 全ドキュメント + GAME_SPEC.md。ブランチ側でdocs等を消してスタート。
    00_setup.md にブランチ運用の手順を記載済み。
